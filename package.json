{
  "name": "aegis-governance",
  "version": "1.0.0",
  "description": "Governance Control for Financial AI Agents",
  "main": "backend/app.py",
  "scripts": {
    "start": "concurrently \"npm --prefix frontend run dev\" \"python3 -m uvicorn backend.app:app --host 0.0.0.0 --port 8000\"",
    "dev": "npm start",
    "backend": "python3 -m uvicorn backend.app:app --host 0.0.0.0 --port 8000",
    "frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
